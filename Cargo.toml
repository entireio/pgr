[package]
name = "pgr"
version = "3.0.0"
edition = "2021"
description = "Stateless MCP server for ranked code search"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[dev-dependencies]
tempfile = "3"
assert_cmd = "2"
